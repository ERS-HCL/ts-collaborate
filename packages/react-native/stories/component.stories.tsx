import React from 'react';
import { LoadingButton } from '../src';

export default { title: 'TS Collaborate/Components/React Native' };

export const LoadingButtonComponent = () => {
  return <LoadingButton />;
};

export const LoadingButtonWithLoadingComponent = () => {
  return <LoadingButton loading />;
};
